#!ipxe

set base-url http://{{ host_ip }}/rocky97

kernel ${base-url}/images/pxeboot/vmlinuz inst.repo=${base-url}/ inst.ks=${base-url}/rocky97_ks.cfg

initrd ${base-url}/images/pxeboot/initrd.img

boot
