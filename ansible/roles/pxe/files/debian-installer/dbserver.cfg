# Debian preseed file #
###                 ###
#####             #####

d-i debian-installer/locale string en_US.UTF-8
d-i debian-installer/language string en
d-i debian-installer/country string DE
d-i debian-installer/allow_unauthenticated string true

d-i keyboard-configuration/xkb-keymap select de
d-i languagechooser/language-name-fb select German
d-i countrychooser/country-name select Germany
d-i console-setup/layoutcode string de_DE

d-i netcfg/choose_interface select auto
d-i netcfg/dhcp_timeout string 5
d-i netcfg/dhcpv6_timeout string 0
d-i netcfg/get_hostname string to-be-set
d-i netcfg/get_domain string devops.army

# Account setup
d-i passwd/root-login boolean true
d-i passwd/make-user boolean false
d-i passwd/root-password-crypted password $6$rounds=60000$wf8v24uCN0bLgIUY$u1shF0cGgCIJEzioJp0ygOPn43HdlxWpWaHfbxpvetcyono.CSEmOC/pA7YP0paRsxpWg06ADN1mxEWSg7ZDl.
d-i user-setup/allow-password-weak boolean false
d-i user-setup/encrypt-home boolean false

# Clock and time zone setup
d-i clock-setup/utc boolean true
d-i time/zone string EU/Berlin
d-i clock-setup/ntp boolean true
d-i clock-setup/ntp-server string 0.pool.ntp.org

# Partition
d-i partman-lvm/device_remove_lvm_span boolean true
d-i partman-auto/purge_lvm_from_device  boolean true
d-i partman-auto/method string lvm
d-i partman-lvm/device_remove_lvm boolean true
d-i partman-md/device_remove_md boolean true
d-i partman-lvm/confirm boolean true
d-i partman-lvm/confirm_nooverwrite boolean true
d-i partman-auto-lvm/no_boot boolean true
d-i partman-auto-lvm/guided_size string max
d-i partman-auto/choose_recipe select custom-lvm
d-i partman/default_filesystem string ext4
d-i partman-auto/disk string /dev/sda
d-i partman-auto-lvm/new_vg_name string vg-server
d-i partman-basicfilesystems/no_swap boolean false

d-i partman-auto/expert_recipe string                   \
      custom-lvm ::                                     \
        40 300 300 ext4                                 \
            $primary{ }                                 \
            $bootable{ }                                \
            method{ format } format{ }                  \
            use_filesystem{ } filesystem{ ext4 }        \
            mountpoint{ /boot }                         \
        .                                               \
        4096 4096 4096 linux-swap                       \
            $primary{ }                                 \
            method{ swap } format{ }                    \
        .                                               \
        5000 70 3000 ext4                               \
            $lvmok{ }                                   \
            method{ format } format{ }                  \
            use_filesystem{ } filesystem{ ext4 }        \
            options/relatime{ relatime }                \
            mountpoint{ / }                             \
        .                                               \
        2000 60 3000 ext4                               \
            $lvmok{ }                                   \
            method{ format } format{ }                  \
            use_filesystem{ } filesystem{ ext4 }        \
            options/relatime{ relatime }                \
            mountpoint{ /usr }                          \
        .                                               \
        8000 80 10000 ext4                              \
            $lvmok{ }                                   \
            method{ format } format{ }                  \
            use_filesystem{ } filesystem{ ext4 }        \
            mountpoint{ /tmp }                          \
            options/relatime{ relatime }                \
            options/nosuid{ nosuid }                    \
            options/nodev{ nodev }                      \
            options/noexec{ noexec }                    \
        .                                               \
        5000 50 10000 ext4                              \
            $lvmok{ }                                   \
            method{ format } format{ }                  \
            use_filesystem{ } filesystem{ ext4 }        \
            mountpoint{ /home }                         \
            options/relatime{ relatime }                \
            options/nosuid{ nosuid }                    \
            options/nodev{ nodev }                      \
        .                                               \
        5000 85 10000 ext4                              \
            $lvmok{ }                                   \
            method{ format } format{ }                  \
            use_filesystem{ } filesystem{ ext4 }        \
            mountpoint{ /var }                          \
            options/relatime{ relatime }                \
        .                                               \
        5000 90 -1 ext4                                 \
            $lvmok{ }                                   \
            method{ format } format{ }                  \
            use_filesystem{ } filesystem{ ext4 }        \
            mountpoint{ /var/lib/mysql }                \
            options/relatime{ relatime }                \
        .

d-i partman/confirm_write_new_label boolean true
d-i partman/choose_partition select Finish partitioning and write changes to disk
d-i partman/confirm boolean true

# Scan another CD or DVD?
apt-cdrom-setup apt-setup/cdrom/set-first boolean false

# Package mirror
#d-i mirror/country string manual
#d-i mirror/http/hostname string ftp.de.debian.org
#d-i mirror/http/directory string /debian
#d-i mirror/http/proxy string
#d-i apt-setup/services-select multiselect security, updates
#d-i apt-setup/security_host string security.debian.org

d-i mirror/country string manual
d-i mirror/http/hostname string ftp.de.debian.org
d-i mirror/http/directory string /debian
d-i mirror/http/proxy string
d-i mirror/suite string trixie
d-i mirror/codename string trixie
d-i apt-setup/services-select multiselect

# Don't send reports back to the project
popularity-contest popularity-contest/participate boolean false

# Package selection
tasksel tasksel/first multiselect ssh-server standard

# Automatically install grub to the MBR
d-i grub-installer/only_debian boolean true
d-i grub-installer/bootdev string default

# Turn off last message about the install being complete
d-i finish-install/reboot_in_progress note
d-i pkgsel/include string openssh-server ntpsec sudo open-vm-tools vim

# do not install recommends
d-i base-installer/install-recommends boolean false

# Set post install stuff
d-i preseed/late_command string cp -a /postseed.sh /target/root ; \
                                in-target chmod +x /root/postseed.sh ; \
                                in-target /root/postseed.sh ; \
                                in-target sed "s/PermitRootLogin\ without-password/PermitRootLogin\ yes/" -i /etc/ssh/sshd_config ; \
                                in-target echo "/tmp /var/tmp none rw,noexec,nosuid,nodev,bind 0 0" >> /etc/fstab
